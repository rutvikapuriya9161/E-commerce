const initVal = {
    isLogin : false,
    user: null,
    error: ''
}

export const authReducer = (state = initVal, action) => {
    switch(action.type){
        default:
            return state;
    }
}